// Import @github/spec-kit from GitHub Packages.
// Ensure GITHUB_TOKEN is set and `npm install` has been run before using this module.
const specKit = require('@github/spec-kit');

module.exports = specKit;
